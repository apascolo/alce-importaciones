export enum eRoutes {
  Root = '',
  Auth = 'auth',
  Login = 'login',
  Products = 'products',
  Categories = 'categories',
  Users = 'users',
  Operations = 'operations',
  Suppliers = 'suppliers',
  Customers = 'customers',
}
