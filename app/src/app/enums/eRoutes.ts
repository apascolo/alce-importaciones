export enum eRoutes {
  Root = '',
  Auth = 'auth',
  Login = 'login',
  Products = 'products',
  Categories = 'categories',
  Users = 'users',
  Operations = 'operations',
  Entities = 'entities',
}
